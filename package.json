{
  "name": "29cm_web",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://minjinworld.github.io/29cm_next",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "lint": "next lint",
    "deploy": "next build && next export && touch out/.nojekyll && git add -f out/ && git commit -m 'deploy to gh-pages' && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "13.2.4"
  },
  "devDependencies": {
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
